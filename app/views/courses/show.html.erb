<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @course.name %></br>
  <%= image_tag @course.image.url(:large) %>
</p>

<%= link_to 'Edit', edit_course_path(@course) %> |
<%= link_to 'Back', courses_path %>
